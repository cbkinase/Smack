version: '3'
services:

  nginx:
    build:
      context: ./nginx
      dockerfile: Dockerfile
    ports:
      - '8080:80'
    depends_on:
      - frontend
      - backend
    restart: always

  frontend:
    build:
      context: ./react-app
      dockerfile: Dockerfile
    depends_on:
      - backend
    environment:
      - HOST=0.0.0.0
    restart: always
    volumes:
      - /home/node/app/node_modules
      - ./react-app:/home/node/app

  backend:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/app
    restart: always
